version: "3.8"

services:
  oneclick:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - oneclick-data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8000
      - HOST=0.0.0.0

volumes:
  oneclick-data:
